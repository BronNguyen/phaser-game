enum PlayerState {
    Waiting = 'Waiting',
    StartTurn = 'StartTurn',
    Rolling = 'Rolling',
    Moving = 'Moving',
    EndTurn = 'EndTurn',
    Win = 'Win',
    Lose = 'Lose'
}

export default PlayerState;